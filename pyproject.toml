[project]
name = "hexlet-code"
version = "0.1.0"
description = "It is Page Analyzer"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.0.0",
    "python-dotenv>=1.2.2",
    "psycopg[binary]>=3.1.0",
    "validators>=0.22.0",
]


[dependency-groups]
dev = [
    "ruff>=0.15.15"
]
